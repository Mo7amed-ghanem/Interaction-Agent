[project]
name = "interaction-agent"
version = "0.1.0"
description = "LangGraph-based User Interaction Agent for BMS"
requires-python = ">=3.10"
dependencies = [
  "pydantic>=2.0.0",
  "langgraph>=0.2.0",
]

[tool.pytest.ini_options]
pythonpath = ["."]
